# SearXNG Docker Compose Environment Configuration
# Copy this file to .env and customize for your setup

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# Port to expose SearXNG on (default: 8888)
SEARXNG_PORT=8888

# Base URL for SearXNG (used for redirects and links)
# Change this if accessing from a different hostname or through a reverse proxy
SEARXNG_BASE_URL=http://localhost:8888

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Secret key for cryptographic operations (REQUIRED for production)
# Generate with: openssl rand -hex 32
# Leave empty to use the default (NOT recommended for production)
SEARXNG_SECRET=

# =============================================================================
# NOTES
# =============================================================================
# 
# For internet-accessible instances:
# 1. Generate a strong secret: openssl rand -hex 32
# 2. Set SEARXNG_SECRET to the generated value
# 3. Update SEARXNG_BASE_URL to your public URL
# 4. Consider using a reverse proxy (nginx/caddy) with TLS
#
# For local-only usage:
# - The defaults should work fine
# - The secret key provides cryptographic security for features like image proxy
